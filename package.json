{
  "name": "course-player-socketio",
  "version": "1.0.0",
  "description": "Course Player built with Socket.IO",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\"",
    "clean": "rm -rf build && mkdir build && mkdir build/apis/ && mkdir build/client/ && mkdir build/model/",
    "copy": "cp apis/* build/apis/ && cp client/* build/client && cp model/* build/model",
    "build": "npm run clean && npm run copy"
  },
  "keywords": [],
  "author": {
    "name": "Rong Zhuang",
    "email": "jojozhuang@gmail.com"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "~0.10.0 || ~0.12.0 || ~9.4.0"
  }
}
